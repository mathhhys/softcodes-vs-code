specVersion: "v1beta"
serviceId: "softcodes-vs-code"
location: "europe-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "softcodes-vs-code-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
